 <!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Meetingup" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'Meetingup' %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
</head>

<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="navbar-header">
      <!-- <img src="/assets/logo vik.png" alt=""  height="50" style="margin-right: 10px; margin-top: 5px; margin-left: 10px"/> -->
      <%= image_tag("logo_cliente.png", height: '50', style:"margin-right: 10px; margin-top: 5px; margin-left: 10px") %>
      <%= link_to icon(:home, 'Meeting up'), root_url, class: 'navbar-brand navbar-brand-centered' %>
    </div>
  </nav>
  <br/><br/>
  <div class="container-fluid">
    <div class="row">
      <div class="<%= content_for?(:class_container) ? yield(:class_container) : ''%>">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </div>
    </div>
  </div>

  <%= javascript_include_tag "devise", "data-turbolinks-track" => true %>

</body>
</html>
